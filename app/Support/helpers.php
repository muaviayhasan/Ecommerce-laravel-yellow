<?php

/*
|--------------------------------------------------------------------------
| Global Helper Functions
|--------------------------------------------------------------------------
|
| Autoloaded by Composer (see the "files" entry in composer.json). Per
| CONVENTIONS.md §6.1, admin-configurable values are read through helpers here
| (format_money(), format_date(), format_time(), format_datetime(), per_page(),
| setting()). Add those as the settings system is built out.
|
*/
