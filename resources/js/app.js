import './bootstrap';

/**
 * jQuery form enhancements (Select2 + jquery-mask) are initialised centrally
 * by erp-forms.js, exposed as `window.ErpForms.init`. See CONVENTIONS.md §1 & §7.
 */
import './erp-forms';
