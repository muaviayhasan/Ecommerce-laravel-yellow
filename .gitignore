/.phpunit.cache
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json
/.fleet
/.idea
/.nova
/.vscode
/.zed
/public/stich

# Locally installed Claude Code skills — personal tooling, not part of the app.
# Scoped to skills/ rather than all of .claude/ so a shared .claude/settings.json
# can still be committed if the team ever wants one.
/.claude/skills

